interface:
  display_name: "Ask User Line Plugin"
  short_description: "Repo-specific patterns and workflows for ask-user-line-plugin"
  default_prompt: "Use the ask-user-line-plugin repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true